# Pinned bounds — chosen so that `pip install -r requirements.txt` works
# self-contained on a clean machine:
#   plotly < 6 + kaleido == 0.2.1 : static chart export (PNG/SVG/PDF) with the
#   bundled renderer. kaleido >= 1.0 requires a separate Chrome install and
#   would crash static export on machines without it.
streamlit>=1.35.0,<2
numpy>=1.26.0,<3
scipy>=1.13.0,<2
pandas>=2.2.0,<3
plotly>=5.22.0,<6
kaleido==0.2.1
openpyxl>=3.1.0,<4
pytest>=8.2.0
pytest-cov>=5.0.0
